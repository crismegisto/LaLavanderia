import React from 'react';
import AwesomeLoading from 'react-native-awesome-loading';

const Loader = () => {
  return <AwesomeLoading indicatorId={5} size={160} isActive={true} />;
};

export default Loader;
